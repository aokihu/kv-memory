/**
 * commitlint configuration for Conventional Commits validation.
 * If commit validation fails, start debugging from this extends preset.
 */
export default {
  extends: ["@commitlint/config-conventional"],
};
